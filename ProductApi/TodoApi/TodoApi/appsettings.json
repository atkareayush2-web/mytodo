{
  "ConnectionStrings": {
    // Connection string to SQL Server database.
   
    "DefaultConnection": "Server=localhost\\SQLEXPRESS;Database=TodoDB;Trusted_Connection=True;TrustServerCertificate=True;"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information", // Normal log level for app
      "Microsoft.AspNetCore": "Warning" // Reduce noise from ASP.NET logs
    }
  },
  "AllowedHosts": "*" // Allows app to run on any host
}
